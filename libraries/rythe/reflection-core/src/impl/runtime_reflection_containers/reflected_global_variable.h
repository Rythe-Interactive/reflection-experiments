#pragma once

// is there a difference between a global variable and a variable in a struct? one is contained in a struct, and the other is contained in a namespace.
struct reflected_global_variable {};
